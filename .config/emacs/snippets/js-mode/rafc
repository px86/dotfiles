# -*- mode: snippet -*-
# name: rafc
# key: rafc
# --
import React from 'react';

const ${1:`(replace-regexp-in-string "\\..*$" "" (file-name-base (buffer-file-name)))`} = (props) => {
    return (
        $0
    );
}

export default $1;
